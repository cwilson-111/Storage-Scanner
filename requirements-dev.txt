# Build-time only. Storage Scanner itself has no third-party runtime
# dependencies (matplotlib is optional, used only for growth-history charts).
pyinstaller>=6.0

# Only needed to regenerate the app icon via make_icon.py (icon.ico is
# already committed, so this is not required just to build the .exe).
pillow>=10.0

# Needed to run the test suite with coverage (pytest tests/) and the quality
# gates CI enforces: ruff (lint + import order, replaces pyflakes), black
# (formatting, checked not applied in CI), and mypy (type contradictions).
# All four are configured in pyproject.toml, so a local run needs no flags.
pytest>=8.0
pytest-cov>=5.0
ruff>=0.16
black>=25.0
mypy>=1.11
